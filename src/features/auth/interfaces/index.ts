export interface IUser {
  is_verified_email: boolean;
  is_active: boolean;
  branch_id: number;
  birth_date: Date;
  deleted_at: null;
  username: string;
  avatar: string;
  gender: string;
  email: string;
  phone: string;
  name: string;
  role: string;
  id: string;
}
