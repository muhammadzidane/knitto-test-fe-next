// Next
import type { Metadata } from "next";

// Global Styles
import "@/assets/styles/sass/_index.scss";

// Redux Provider
import ReduxProvider from "@/plugins/redux/provider";

// Middleware
import AuthMiddleware from "./middleware/auth";

// Interfaces
import { type IChildrenProps } from "@/features/app/interfaces";

export const metadata: Metadata = {
  title: "POSME",
  description: "Generated by create next app",
};

const RootLayout: React.FC<IChildrenProps> = ({ children }) => (
  <html lang="en">
    <body>
      <ReduxProvider>
        <AuthMiddleware>{children}</AuthMiddleware>
      </ReduxProvider>
    </body>
  </html>
);

export default RootLayout;
